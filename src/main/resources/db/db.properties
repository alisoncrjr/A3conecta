# DB defaults ? override with environment variables
db.host=localhost
db.port=3306
db.name=conecta
db.user=root
db.pass=123456
db.pool.size=10
# Enable DB by default when present; can be overridden with USE_DB env var
db.enabled=true